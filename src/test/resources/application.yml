spring:
  jpa:
    ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: org.hsqldb.jdbcDriver
    url: jdbc:hsqldb:mem:db
    platform: HSQL
    username: sa
    password: sa
